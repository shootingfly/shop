# require "spec"
# require "../src/orm"
# DB_URL = "mysql://rocky@localhost/shop"
# # attributes(User, [name, bar, so], String)
# # attributes(Product, [name, id], String)
# # attributes(Post, [id, name, content], String)
# # attributes(Dog, [name, content], String)
# # User.table_name = "users"
# table User, name: String, bar: String
# table Cat, name: String, bar: Int32
